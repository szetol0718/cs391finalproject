import { createTheme } from '@mui/material/styles'; //didn't like the blue focus thing


const theme = createTheme({
    palette: {
        primary: {
            main: '#5571b8',
        },
    },

});
export default theme;